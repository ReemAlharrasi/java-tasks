package task386;

public class FindMaximumInArray {
    /*void main() {
        System.out.println("Find the Maximum in an Array");

        int result;
        result = findMax(new int[]{3, 7, 2, 9, 4},0   );
        System.out.println("{[3, 7, 2, 9, 4]} : " + result);

        result = findMax(new int[]{1, 1, 1}, 0);
        System.out.println("{1, 1, 1}: " + result);

        result = findMax(new int[]{-5, -2, -8}, 0);
        System.out.println("{-5, -2, -8}: " + result);

        result = findMax(new int[]{42}, 0);
        System.out.println("{42}: " + result);
    }

    public static int findMax(int[] numbers, int max) {
        if (index == numbers.length) return 0; //done iterating
        if (numbers[index])
        return numbers[index] + findMax(numbers, index + 1);
    }*/
}
